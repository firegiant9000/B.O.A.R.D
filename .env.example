# Firebase configuration — production injection (Month 2, Phase 1).
#
# In development these are read from `expo.extra.firebase` in app.json (committed
# dev project). For a production build, set these EXPO_PUBLIC_* vars (in EAS build
# secrets or a local `.env`, which is gitignored) — they override the app.json
# values. Expo inlines EXPO_PUBLIC_* vars into the bundle at build time.
#
# The Firebase web apiKey is a public client identifier, not a secret; access is
# gated by Firestore/Storage security rules.

EXPO_PUBLIC_FIREBASE_API_KEY=
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=
EXPO_PUBLIC_FIREBASE_PROJECT_ID=
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
EXPO_PUBLIC_FIREBASE_APP_ID=

# Sentry error reporting (Month 2, Phase 2).
#
# EXPO_PUBLIC_SENTRY_DSN is read at runtime by src/lib/errorReporting.ts. When set
# (standalone preview/production builds), exceptions forward to Sentry; when empty
# (local dev / Expo Go) the seam falls back to console logging. The DSN is a public
# client identifier, not a secret.
EXPO_PUBLIC_SENTRY_DSN=

# SENTRY_AUTH_TOKEN is used at BUILD time only (by the Sentry Expo config plugin) to
# upload source maps for readable stack traces. It IS a secret — set it as an EAS
# secret (`eas secret:create`), never commit it.
SENTRY_AUTH_TOKEN=

# Universal / App Link domain (Month 2, Phase 4).
#
# Apex host used by src/lib/deepLinks.ts to build https invite links
# (https://<domain>/b/{inviteCode}). While unset it falls back to the
# LINK_DOMAIN_PLACEHOLDER "boardapp.example.com" and `hasLinkDomain()` stays false —
# links are still generated, they just point at a domain nobody owns, so a tap
# resolves to nothing. Setting this is only half the job: the same real domain must
# also replace the placeholder in app.json (`ios.associatedDomains` + the Android
# `autoVerify` intent filter) and the two files in public/.well-known/ must be served
# from it with the real Apple Team ID and EAS SHA-256 fingerprint. See README →
# "Deep linking".
EXPO_PUBLIC_LINK_DOMAIN=

# Google Sign-In OAuth client IDs (Month 3, Phase 11).
#
# Created in the Google Cloud Console (one OAuth client per platform) for the same
# project as Firebase. Read by src/services/authProviders.ts; inlined into the
# bundle at build time. While blank, getProvider("google").isAvailable is false and
# the "Continue with Google" button stays hidden — so leaving these empty is safe.
#
# These are public client identifiers, not secrets. The OAuth redirect only works in
# a native build (TestFlight / Play internal), not Expo Go — register the iOS
# reverse-client-id URL scheme and the Android signing SHA-1 in the console.
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=

# AI gateway cutover (Month 4, Phase 1).
#
# When "1", the client calls the `generateSummary` Cloud Function (no OpenAI key on
# device). When unset/0 (the default), it uses the legacy direct-OpenAI path with a
# client-held key. Leave OFF until the function is deployed and verified in prod
# (see docs/functions-deploy-runbook.md), then flip to "1" to cut over. The OpenAI
# key itself is NOT set here — it lives only in the Functions runtime secret
# (`firebase functions:secrets:set OPENAI_API_KEY`), never in the client bundle.
EXPO_PUBLIC_AI_GATEWAY=

# Handwriting OCR (Month 4, Phase 10).
#
# When "1", exposes the "Recognize text" affordance on a stroke selection and calls
# the `recognizeHandwriting` Cloud Function (results are memoized against a hash of
# the path IDs, so re-running a selection is free). Leave OFF until the function is
# deployed AND the Google Vision key is set as a Functions runtime secret
# (`firebase functions:secrets:set GOOGLE_VISION_API_KEY`). Rides the same gateway as
# summaries, so it is only meaningful with EXPO_PUBLIC_AI_GATEWAY=1.
EXPO_PUBLIC_OCR=

# Explain selection (Month 4, Phase 11).
#
# When "1", exposes the "Explain this" affordance on a board selection and calls
# the `explainSelection` Cloud Function. Leave OFF until the function is deployed.
# Rides the same gateway as summaries, so it is only meaningful with
# EXPO_PUBLIC_AI_GATEWAY=1.
EXPO_PUBLIC_EXPLAIN=

# Text → diagram (Month 4, Phase 12).
#
# When "1", exposes the "Diagram" prompt affordance on a board and calls the
# `textToDiagram` Cloud Function (returns Mermaid, parsed client-side into native
# shapes). Leave OFF until the function is deployed. Rides the same gateway as
# summaries, so it is only meaningful with EXPO_PUBLIC_AI_GATEWAY=1.
EXPO_PUBLIC_DIAGRAM=

# Flashcard generation (Month 6).
#
# When "1", exposes the "Make flashcards" affordance on a board selection and
# calls the `generateFlashcards` Cloud Function. Leave OFF until the function
# is deployed. Rides the same gateway as summaries, so it is only meaningful
# with EXPO_PUBLIC_AI_GATEWAY=1. The review screen and CSV export are NOT
# gated by this flag — reviewing/exporting cards you already have needs no AI
# call and should keep working even with generation switched off.
EXPO_PUBLIC_FLASHCARDS=

# Board Q&A — chat with your board (Month 6).
#
# When "1", exposes the sidebar chat affordance and calls the `askBoard`
# Cloud Function. Leave OFF until the function is deployed. Rides the same
# gateway as summaries, so it is only meaningful with EXPO_PUBLIC_AI_GATEWAY=1.
EXPO_PUBLIC_BOARD_QA=

# Math elements — LaTeX rendered to SVG path data (Month 6).
#
# When "1", exposes the toolbar's equation button and calls the `renderMath`
# Cloud Function. Leave OFF until the function is deployed. UNLIKE every AI
# flag above, this one does NOT depend on EXPO_PUBLIC_AI_GATEWAY: MathJax runs
# in-process with no provider, no API key and no per-call spend, so it is
# neither gated by the gateway cutover nor metered against the workspace's
# AI-call quota — see src/lib/featureFlags.ts's MATH_ENABLED for the full
# reasoning.
EXPO_PUBLIC_MATH=

# Code elements — syntax-highlighted source, tokenized on-device (Month 6).
#
# When "1", exposes the toolbar's code button. UNLIKE every AI flag above and
# LIKE EXPO_PUBLIC_MATH, this does NOT depend on EXPO_PUBLIC_AI_GATEWAY: Shiki
# tokenizes entirely client-side with no Cloud Function, no provider and no
# per-call spend — see src/lib/featureFlags.ts's CODE_ENABLED for the full
# reasoning.
EXPO_PUBLIC_CODE=

# Analytics (Month 6). Read by src/services/analyticsService.ts.
#
# EXPO_PUBLIC_POSTHOG_KEY is a PostHog *project* API key (format "phc_..."),
# public/publishable by design — it identifies a project, not a person; the
# ingestion endpoint is PostHog's own enforcement boundary. A PostHog
# *personal/API* key is a different, secret credential and must never be set
# here or anywhere in client code.
#
# While EXPO_PUBLIC_POSTHOG_KEY is unset (true today — the PostHog project
# gate is not yet met), track()/identifyWorkspace() no-op rather than reach
# the vendor at all; this is the only path exercised in this app right now.
EXPO_PUBLIC_POSTHOG_KEY=

# Ingestion host matching the project above (e.g. PostHog Cloud US/EU, or a
# self-hosted instance). Unset falls back to PostHog's US cloud default.
EXPO_PUBLIC_POSTHOG_HOST=
